# AstraNav-LRIS — Routing & Intelligence Backend
# Team Aura++ | PS-08 | Bharatiya Antariksh Hackathon 2026

# ── Core science / geospatial ─────────────────────────────────────────────────
numpy>=1.26.0
scipy>=1.13.0
scikit-learn>=1.5.0
rasterio>=1.3.10          # GeoTIFF raster I/O (Step 3: API layer)
shapely>=2.0.0            # polygon ops (Step 3: precise rasterisation)
pyproj>=3.6.0             # coordinate reference system transforms

# ── Web framework ─────────────────────────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
pydantic-settings>=2.3.0  # env-var config management
python-multipart>=0.0.9

# ── HTTP client (for live Member 1 calls) ─────────────────────────────────────
httpx>=0.27.0

# ── LLM (copilot feature) ─────────────────────────────────────────────────────
google-generativeai>=0.7.0

# ── Database (PostGIS / SQLite fallback) ─────────────────────────────────────
sqlalchemy>=2.0.0
asyncpg>=0.29.0           # async PostgreSQL driver
aiosqlite>=0.20.0         # async SQLite fallback

# ── Testing ───────────────────────────────────────────────────────────────────
pytest>=8.2.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0
httpx>=0.27.0             # TestClient for FastAPI
reportlab>=4.2.0
